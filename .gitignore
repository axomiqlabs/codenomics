node_modules/
dist/
*.tgz
data/
.DS_Store

# internal-only: agent steering + private deploy runbook (never public)
.claude/
docs/deploy-cloudflare.md

# captured screenshots (brand assets under */assets/brand are tracked explicitly)
screenshots/
*[Ss]creenshot*
Screen Shot *
dash-*.png
